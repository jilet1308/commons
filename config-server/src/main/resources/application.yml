server:
  port: 9001

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_SEARCH_PATH:file:///home/ismail/IdeaProjects/commons/z_configs}
  main:
    banner-mode: off